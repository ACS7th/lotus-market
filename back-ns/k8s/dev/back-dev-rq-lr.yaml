apiVersion: v1
kind: ResourceQuota
metadata:
  name: back-dev-rq
  namespace: back-dev
spec:
  hard:
    requests.cpu: "1"
    requests.memory: "1Gi"
    limits.cpu: "1"
    limits.memory: "2Gi"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: back-dev-lr
  namespace: back-dev
spec:
  limits:
  - type: Container
    defaultRequest:
      cpu: "1"
      memory: "1Gi"
    default:
      cpu: "1"
      memory: "1Gi"
    max:
      cpu: "1"
      memory: "2Gi"
